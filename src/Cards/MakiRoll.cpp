#include "MakiRoll.h"

MakiRoll::MakiRoll(int rollCount) : rollCount(rollCount) {}

int MakiRoll::getPoints() const {
    // Points calculation logic based on game rules
    return 0;
}

int MakiRoll::getRollCount() const {
    return rollCount;
}